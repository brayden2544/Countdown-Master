# Countdown API (Java)

## Endpoints

The following endpoints are currently setup:

  - /user (POST)
  - /user/{id}/video (POST)
  - /user/{id}/checkin (POST)
  - /user/{id}/like (POST)
  - /user/{id}/pass (POST)
